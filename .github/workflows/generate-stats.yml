name: Generate Stats Card

on:
  # Run daily at 00:00 UTC
  schedule:
    - cron: '0 0 * * *'

  # Allow manual trigger
  workflow_dispatch:

  # Run on push to main branch
  push:
    branches:
      - main
    paths:
      - 'generate-stats-card.go'
      - '.github/workflows/generate-stats.yml'

jobs:
  generate-stats-card:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.23'
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Run tests
        run: go test -v -race -coverprofile=coverage.out ./...

      - name: Generate stats card
        env:
          GITHUB_TOKEN: ${{ secrets.STATS_TOKEN || secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: gangwgr
          OUTPUT_DIR: .
          VERBOSE: true
        run: |
          go run generate-stats-card.go

      - name: Verify output
        run: |
          if [ ! -f stats-card.svg ]; then
            echo "Error: stats-card.svg was not generated"
            exit 1
          fi
          echo "Stats card generated successfully"
          ls -lh stats-card.svg

      - name: Commit and push if changed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

          # Fetch the output branch if it exists
          git fetch origin output:output 2>/dev/null || true

          # Check out output branch (create if it doesn't exist)
          if git show-ref --verify --quiet refs/heads/output; then
            git checkout output
            git pull origin output || true
          else
            git checkout --orphan output
            # Remove all files from the index for a clean orphan branch
            git rm -rf . 2>/dev/null || true
          fi

          # Copy stats card to output branch
          git checkout main -- stats-card.svg

          # Add the stats card
          git add -f stats-card.svg

          # Check if there are changes to commit
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update stats card - $(date +'%Y-%m-%d %H:%M:%S UTC')"
            git push origin output
            echo "Stats card updated successfully"
          fi

      - name: Update coverage badge (optional)
        if: github.ref == 'refs/heads/main'
        run: |
          echo "Coverage report generated"
          go tool cover -func=coverage.out
